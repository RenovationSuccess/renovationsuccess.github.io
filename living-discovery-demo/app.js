(()=>{"use strict";const $=s=>document.querySelector(s),app=$("#app"),ins=$("#inspector"),toast=$("#toast"),KEY="gr_living_demo_v5";const AREAS=["三軒茶屋","高津","学芸大学","経堂","下北沢","荻窪","中野","清澄白河","武蔵小杉","大井町","浦和","船橋","吉祥寺","溝の口","桜新町","戸越銀座","赤羽","横浜","柏","大宮","新宿","渋谷","池袋","浅草","品川"];const PH=["photo-1600566753190-17f0baa2a6c3","photo-1600607687920-4e2a09cf159d","photo-1600210492486-724fe5c67fb0","photo-1616486338812-3dadae4b4ace","photo-1600585154340-be6161a56a0c","photo-1600566753086-00f18fb6b3ea","photo-1600585152915-d208bec867a1","photo-1600607687939-ce8a6c25118c","photo-1600566753051-f0b89df2dd90"];const traits=["brightness","openness","interior","walkable","quiet","work","kitchen","storage","community"],labels={walkable:"街の雰囲気",interior:"内装の手触り",brightness:"光の入り方",openness:"抜けと開放感",quiet:"静けさ",work:"家で働く時間",kitchen:"料理のしやすさ",storage:"収納",community:"人とつながる余白"};const TYPE_INFO={standard:{label:"一般賃貸",term:"2年〜",desc:"自分の家具で、腰を据えて暮らす"},tomos:{label:"TOMOS",term:"2年〜",desc:"無垢床と白い空間。goodroomのリノベーション賃貸",original:true},hotel:{label:"ホテル",term:"3泊〜",desc:"必要な日数だけ、身軽に泊まって暮らす"},residence:{label:"goodroom residence",term:"1ヶ月〜",desc:"家具家電と共用部が揃う、goodroomの暮らし",original:true},monthly:{label:"マンスリー",term:"1ヶ月〜",desc:"家具家電つき。引越し準備なしで始める"},coliving:{label:"コリビング",term:"半年〜",desc:"個室と充実した共用部を行き来する暮らし"}};const DURATIONS=[{v:3,label:"3泊",days:3},{v:14,label:"2週間",days:14},{v:30,label:"1ヶ月",days:30},{v:90,label:"3ヶ月",days:90},{v:180,label:"半年",days:180},{v:365,label:"1年",days:365},{v:730,label:"2年以上",days:730}];const RESIDENCES=["戸塚","大阪豊中","和光","西船橋","奈良","荻窪","東小金井","調布国領","渋谷道玄坂 VILLAGE produced by 東急不動産","保土ケ谷","学芸大学","板橋ときわ台","中浦和","越谷","座間ホシノタニ団地","蒲田","亀戸","川崎","大宮","横浜"];function rng(seed){let x=seed;return()=>((x=Math.imul(48271,x)%2147483647)&2147483647)/2147483647}function img(i,w=900){return`https://images.unsplash.com/${PH[i%PH.length]}?auto=format&fit=crop&w=${w}&q=82`}function editorialTitle(p){const f=p.features;if(p.type==="residence")return["サウナと珈琲、駅のそば。","帰る場所が、ひとつ増える。","ラウンジのある日常。","家具を持たずに、暮らしを持つ。"][p.index%4];if(p.type==="tomos")return["無垢床を、素足で。","白と木のあいだで暮らす。","古さの先に、好きがある。","小さくても、気持ちよく。"][p.index%4];if(p.type==="hotel")return["三泊だけ、この街の住人。","朝食のあと、仕事へ。","旅と暮らしの、まんなか。","今週はここから始める。"][p.index%4];if(p.type==="monthly")return["鞄ひとつで、来月から。","暮らしの助走期間。","次の住まいが決まるまで。","家具を選ばない自由。"][p.index%4];if(p.type==="coliving")return["ひとりの時間と、誰かの気配。","リビングは、少し大きめで。","閉じすぎないひとり暮らし。","街の中に、居場所を持つ。"][p.index%4];if(f.openness>.84)return"窓辺に、余白をひとつ。";if(f.interior>.84)return"古さの先に、好きがある。";if(f.brightness>.82)return"朝が似合う部屋。";if(f.walkable>.82)return"寄り道したくなる帰り道。";if(f.quiet>.82)return"静けさを連れて帰る。";return"ここから始まる、ふつうの毎日。"}function story(p){const intro=p.type==="hotel"?"チェックインを済ませたら、その日からこの街での暮らしが始まります。":p.type==="residence"?"部屋の扉を開ける前から、ここでの暮らしは始まっています。ラウンジで仕事をしたり、サウナで一日をほどいたり。":p.type==="tomos"?"玄関を開けると、無垢床のやわらかな表情が目に入ります。新しさだけではない、時間とともに育つ内装です。":p.type==="monthly"?"家具も家電も、暮らしに必要なものはあらかじめ揃っています。荷物を増やさず、次の生活を始められます。":p.type==="coliving"?"個室に戻れば、自分だけの時間。扉の外には、広いリビングと誰かの気配があります。":"朝、カーテンを開けるところから、この部屋の一日は始まります。";return`${intro}\n\n${p.size}㎡という数字だけでは分からない、光の入り方や視線の抜け。写真を眺めているうちに、置きたい家具や過ごしたい時間が少しずつ見えてきます。\n\n${p.area}の街には、帰り道に寄りたくなる場所があります。部屋だけでなく、その周りまで含めて暮らしを選びたい人へ。`}function makeFeatures(r,type){const f={};traits.forEach(t=>f[t]=Math.round((.2+r()*.8)*100)/100);if(type==="tomos"){f.interior=.96;f.brightness=Math.max(.65,f.brightness)}if(type==="residence"){f.community=.92;f.work=.8;f.openness=.75}if(type==="coliving"){f.community=.95;f.work=.78}if(type==="hotel")f.storage=.25;return f}function baseProp(r,i,type,name){const area=name||AREAS[Math.floor(r()*AREAS.length)],size=Math.round((type==="hotel"?12+r()*20:type==="coliving"?7+r()*15:16+r()*39)*10)/10,walk=1+Math.floor(r()*15),age=Math.floor(r()*42),layout=type==="hotel"?"ホテル客室":type==="coliving"?"個室":size<23?"1R":size<33?"1K":size<45?"1LDK":"2LDK";let rent=60000+Math.floor(r()*150)*1000;if(type==="hotel")rent=4500+Math.floor(r()*120)*100;if(type==="residence")rent=49800+Math.floor(r()*145)*1000;if(type==="monthly")rent=75000+Math.floor(r()*125)*1000;if(type==="coliving")rent=65000+Math.floor(r()*80)*1000;const minDays=type==="hotel"?3:(type==="residence"||type==="monthly")?30:type==="coliving"?180:730,furnished=["hotel","residence","monthly","coliving"].includes(type),common=["residence","coliving"].includes(type),f=makeFeatures(r,type),p={id:`${type.toUpperCase()}-${String(i+1).padStart(4,"0")}`,index:i,type,area,rent,size,walk,age,layout,minDays,furnished,common,station:`${area}駅`,photos:[img(i),img(i+1),img(i+2)],features:f};p.title=editorialTitle(p);p.story=story(p);return p}function makeInventory(){const r=rng(24681357),all=[];for(let i=0;i<800;i++){let p=baseProp(r,i,"standard");if(i<230)p.rent=60000+Math.floor(r()*40)*1000;else if(i<460)p.rent=100000+Math.floor(r()*50)*1000;else p.rent=150000+Math.floor(r()*130)*1000;all.push(p)}for(let i=0;i<200;i++){let p=baseProp(r,i,"tomos");p.rent=70000+Math.floor(r()*110)*1000;all.push(p)}for(let i=0;i<130;i++)all.push(baseProp(r,i,"hotel"));RESIDENCES.forEach((n,i)=>all.push(baseProp(r,i,"residence",n)));for(let i=0;i<100;i++)all.push(baseProp(r,i,"monthly",["新宿","渋谷","池袋","品川","浅草","大井町","中野","荻窪"][i%8]));for(let i=0;i<20;i++)all.push(baseProp(r,i,"coliving",["下北沢","高円寺","阿佐ヶ谷","三軒茶屋","清澄白河"][i%5]));return all}const PROPS=makeInventory();function uid(p){return p+Math.random().toString(36).slice(2,10)}function base(){return{guestId:uid("guest_"),userId:null,screen:"home",filters:{area:"",maxRent:150000,type:"all",duration:30},favorites:[],compare:[],events:[],profile:{walkable:.48,interior:.5,brightness:.45,openness:.42,quiet:.35,work:.35,kitchen:.3,storage:.3,community:.25},selectedReasons:[]}}let state=load();function load(){try{return Object.assign(base(),JSON.parse(localStorage.getItem(KEY)||"{}"))}catch(e){return base()}}function save(){localStorage.setItem(KEY,JSON.stringify(state))}function event(type,data={}){state.events.unshift({type,time:new Date().toLocaleTimeString("ja-JP",{hour:"2-digit",minute:"2-digit"}),...data});state.events=state.events.slice(0,30);save()}function go(s){state.screen=s;event("screen",{to:s});render();app.scrollTop=0}function price(p){return p.type==="hotel"?`${(p.rent*3).toLocaleString()}円 / 3泊`:`${p.rent.toLocaleString()}円 / 月`}function eligible(p){return state.filters.duration>=p.minDays}function filtered(){return PROPS.filter(p=>(!state.filters.area||p.area.includes(state.filters.area))&&(state.filters.type==="all"||p.type===state.filters.type)&&eligible(p)&&(p.type==="hotel"||p.rent<=state.filters.maxRent))}function score(p){let s=0,w=0;traits.forEach(k=>{const wt=state.profile[k]||.2;s+=wt*p.features[k];w+=wt});if(eligible(p))s+=.35;if(p.type!=="hotel"&&p.rent<=state.filters.maxRent)s+=.25;if(p.type==="residence"||p.type==="tomos")s+=.08;return Math.max(0,Math.min(1,s/(w+.68)))}function recs(){return [...PROPS].filter(eligible).sort((a,b)=>score(b)-score(a)).slice(0,14)}function reason(p){const top=Object.entries(p.features).sort((a,b)=>state.profile[b[0]]*b[1]-state.profile[a[0]]*a[1]).slice(0,2).map(([k])=>labels[k]);return`これまで見てきた住まいには「${top[0]}」と「${top[1]}」という共通点がありました。`}function brand(p){const t=TYPE_INFO[p.type];return t.original?`<div class="brand-mark">goodroom ORIGINAL　${t.label}</div>`:`<div class="type-line">${t.label}　${t.term}</div>`}function shell(body,active="home"){return`<header class="topbar"><button class="logo" data-go="home">goodroom</button><div class="header-actions"><button class="ghost" data-go="insight">気づき</button><button class="ghost" data-login>${state.userId?"保存済み":"♡ 保存"}</button></div></header>${body}<nav class="nav"><button class="${active==="home"?"on":""}" data-go="home">ホーム</button><button class="${active==="search"?"on":""}" data-go="search">さがす</button><button class="${active==="recommend"?"on":""}" data-go="recommend">見つける</button><button class="${active==="compare"?"on":""}" data-go="compare">比較 ${state.compare.length}</button></nav>`}function actions(p){return`<div class="mini-actions"><button class="text-action ${state.favorites.includes(p.id)?"on":""}" data-fav="${p.id}">${state.favorites.includes(p.id)?"♥ 保存済み":"♡ 保存"}</button><button class="text-action ${state.compare.includes(p.id)?"on":""}" data-comp="${p.id}">${state.compare.includes(p.id)?"比較中":"＋ 比較"}</button></div>`}function property(p,why=false){return`<article class="property ${TYPE_INFO[p.type].original?"original-property":""}" data-open="${p.id}"><img src="${p.photos[0]}" alt=""><div>${brand(p)}<h3>${p.title}</h3><div class="meta">${p.area}　${p.station} 徒歩${p.walk}分<br>${p.layout} / ${p.size}㎡${p.furnished?"　家具家電つき":""}${p.common?"　共用部あり":""}</div><div class="price">${price(p)}</div>${why?`<div class="reason">${reason(p)}</div>`:""}${actions(p)}</div></article>`}function filterUI(){return`<div class="search-axis"><label class="field"><span>エリア</span><select id="area"><option value="">首都圏すべて</option>${AREAS.map(a=>`<option value="${a}" ${a===state.filters.area?"selected":""}>${a}</option>`).join("")}</select></label><label class="field"><span>住む期間</span><select id="duration">${DURATIONS.map(d=>`<option value="${d.days}" ${d.days===+state.filters.duration?"selected":""}>${d.label}</option>`).join("")}</select></label><label class="field"><span>住まいの種類</span><select id="type"><option value="all">すべて</option>${Object.entries(TYPE_INFO).map(([k,v])=>`<option value="${k}" ${k===state.filters.type?"selected":""}>${v.label}</option>`).join("")}</select></label><label class="field"><span>月額上限</span><select id="rent">${[80000,100000,150000,200000,300000].map(n=>`<option value="${n}" ${n===+state.filters.maxRent?"selected":""}>${n.toLocaleString()}円</option>`).join("")}</select></label><button class="btn primary" id="searchBtn">この条件で見る</button></div>`}function home(){const originals=PROPS.filter(p=>p.type==="residence").slice(0,4),tomos=PROPS.filter(p=>p.type==="tomos").slice(0,4);return shell(`<div class="hero"><img src="${img(0,1200)}"><div class="hero-copy"><h1>三泊から、何年先まで。<br>暮らし方から住まいを探す。</h1><p>ホテルも、家具つきも、リノベーション賃貸も。同じ場所で比べられます。</p></div></div><main class="page"><section class="section"><h2 class="blue-title">どれくらい、どんなふうに住みますか？</h2>${filterUI()}</section><section class="section"><h2 class="blue-title">期間から、選べる住まいが変わります。</h2><div class="duration-guide">${DURATIONS.slice(0,6).map(d=>`<button data-duration="${d.days}"><b>${d.label}</b><span>${d.days<30?"ホテル":d.days<180?"ホテル・マンスリー・residence":d.days<730?"家具つき・コリビング": "賃貸・TOMOS"}</span></button>`).join("")}</div></section><section class="section original-section"><div class="brand-heading">goodroom ORIGINAL</div><h2 class="blue-title">goodroom residence</h2><p class="lead">1ヶ月から。家具家電つきの個室に、ラウンジやサウナなどの共用部。部屋の外まで含めて暮らしをつくる、goodroomのオリジナルレジデンスです。</p><div class="editorial-grid">${originals.map(p=>`<article class="editorial-item" data-open="${p.id}"><img src="${p.photos[0]}"><h3>${p.area}</h3><div class="meta">1ヶ月〜 / 家具家電つき / 共用部充実<br>${price(p)}</div></article>`).join("")}</div><button class="text-link" data-brand="residence">20棟をすべて見る →</button></section><section class="section original-section"><div class="brand-heading">goodroom ORIGINAL</div><h2 class="blue-title">TOMOS</h2><p class="lead">無垢床、白い壁、使いやすい水まわり。古い部屋の良さを残しながら、今の暮らしへ整えたgoodroomのリノベーションブランドです。</p><div class="editorial-grid">${tomos.map(p=>`<article class="editorial-item" data-open="${p.id}"><img src="${p.photos[0]}"><h3>${p.title}</h3><div class="meta">${p.area} / ${price(p)}</div></article>`).join("")}</div><button class="text-link" data-brand="tomos">TOMOS 200件を見る →</button></section><section class="section"><h2 class="blue-title">住まいの種類から考える。</h2><div class="choice-list">${Object.entries(TYPE_INFO).map(([k,v])=>`<button class="choice" data-brand="${k}"><span><strong>${v.label}</strong><br>${v.term}　${v.desc}</span></button>`).join("")}</div></section></main>`,"home")}function search(){const list=filtered().slice(0,18);return shell(`<main class="page"><section class="section"><h1 class="blue-title">期間と暮らし方から探す</h1>${filterUI()}</section><section class="section"><h2 class="blue-title">${filtered().length}件の住まい</h2><div class="property-list">${list.map(p=>property(p)).join("")||`<div class="empty">この期間と条件で住める在庫がありません。</div>`}</div></section></main>`,"search")}function quiz(){const ps=[PROPS.find(p=>p.type==="hotel"),PROPS.find(p=>p.type==="residence"),PROPS.find(p=>p.type==="tomos")];return shell(`<main class="page"><section class="section"><h1 class="blue-title">今の自分に近い暮らしは、どれですか？</h1><p class="lead">部屋の好みだけでなく、住む期間や持ち物との付き合い方も選択の一部です。</p></section><section class="section"><div class="editorial-grid">${ps.map(p=>`<article class="editorial-item" data-pick="${p.id}"><img src="${p.photos[0]}">${brand(p)}<h3>${p.title}</h3><div class="meta">${TYPE_INFO[p.type].term} / ${TYPE_INFO[p.type].desc}</div></article>`).join("")}</div></section></main>`,"recommend")}function reasons(){return shell(`<main class="page"><section class="section"><h1 class="blue-title">何に惹かれましたか？</h1><div class="choice-list">${Object.entries(labels).slice(0,7).map(([k,l])=>`<button class="choice" data-reason="${k}"><span><strong>${state.selectedReasons.includes(k)?"✓ ":""}${l}</strong></span></button>`).join("")}</div></section><button class="btn primary wide" data-finish>見えてきたことを読む</button></main>`,"recommend")}function insight(){const r=Object.entries(state.profile).sort((a,b)=>b[1]-a[1]);return shell(`<main class="page"><section class="section"><h1 class="blue-title">部屋だけでなく、住み方にも好みがありそうです。</h1><p class="story">あなたが見てきた住まいには「${labels[r[0][0]]}」と「${labels[r[1][0]]}」という共通点がありました。\n\n契約期間や家具の有無を固定せずに探すと、これまで候補に入らなかった住まいにも同じ価値が見つかります。</p></section><section class="section"><div class="property-list">${recs().slice(0,8).map(p=>property(p,true)).join("")}</div></section></main>`,"recommend")}function recommend(){return shell(`<main class="page"><section class="section"><h1 class="blue-title">契約のかたちを越えて、こんな暮らしも。</h1><p class="lead">一般賃貸だけでなく、ホテル、家具つき、コリビング、goodroomのオリジナルブランドを横断して提案します。</p></section><section class="section"><div class="property-list">${recs().map(p=>property(p,true)).join("")}</div></section></main>`,"recommend")}function map(p){return`<div class="map"><svg viewBox="0 0 500 290"><rect width="500" height="290" fill="#edf1ec"/><path d="M0 60 C120 35 160 105 280 70 S410 35 500 60" stroke="#c8dbea" stroke-width="20" fill="none"/><path d="M55 0L135 290M250 0L205 290M400 0L330 290" stroke="#fff" stroke-width="12"/><path d="M0 205L500 170M0 125L500 140" stroke="#fff" stroke-width="9"/><text x="18" y="275" font-size="13" fill="#68766c">${p.area}周辺・デモ地図</text></svg><div class="map-pin"></div></div>`}function detail(id){const p=PROPS.find(x=>x.id===id);if(!p)return home();return shell(`<main class="page"><section class="gallery"><img class="gallery-main" src="${p.photos[0]}"><div class="thumbs"><img src="${p.photos[1]}"><img src="${p.photos[2]}"></div></section><section class="detail-head">${brand(p)}<h1 class="detail-title">${p.type==="residence"?`goodroom residence ${p.area}`:p.title}</h1><div class="detail-price">${price(p)}</div><div class="spec-line">${TYPE_INFO[p.type].term} / ${p.layout} / ${p.size}㎡ / ${p.area}</div><div class="spec-line">${p.furnished?"家具家電つき　":""}${p.common?"ラウンジなど共用部あり":""}</div>${actions(p)}</section><section class="section"><h2 class="blue-title">この住まいの紹介</h2><p class="story">${p.story}</p></section>${TYPE_INFO[p.type].original?`<section class="section original-detail"><div class="brand-heading">goodroom ORIGINAL</div><h2 class="blue-title">このブランドだからできること</h2><p class="story">${p.type==="residence"?"部屋だけを貸すのではなく、働く、休む、ととのう場所まで一緒につくる。家具家電と共用部を備え、1ヶ月から暮らせる住まいです。":"素材を選び、間取りを整え、その部屋にしかない魅力を残す。TOMOSは、goodroomが設計から考えるリノベーション賃貸です。"}</p></section>`:""}<section class="section"><h2 class="blue-title">契約と暮らしの条件</h2><div class="values"><div class="value-row"><b>利用期間</b><span>${TYPE_INFO[p.type].term}</span></div><div class="value-row"><b>料金</b><span>${price(p)}</span></div><div class="value-row"><b>家具・家電</b><span>${p.furnished?"備え付け。入居日から生活できます":"なし。自分の家具で暮らします"}</span></div><div class="value-row"><b>共用部</b><span>${p.common?"ラウンジ、ワークスペースなどを利用できます":"専有部を中心とした暮らしです"}</span></div></div></section><section class="section"><h2 class="blue-title">街と暮らす</h2>${map(p)}</section><div style="height:80px"></div><div class="sticky-actions"><button class="btn outline" data-fav="${p.id}">保存する</button><button class="btn green" data-comp="${p.id}">比較に追加</button></div></main>`,"search")}function compare(){const ps=state.compare.map(id=>PROPS.find(p=>p.id===id)).filter(Boolean).slice(0,3);return shell(`<main class="page"><section class="section"><h1 class="blue-title">契約の違いまで、同じ表で比べる。</h1></section>${ps.length<2?`<div class="empty">気になる住まいを2件以上追加してください。</div>`:`<section class="section"><table class="compare-table"><tr><th></th>${ps.map(p=>`<th>${TYPE_INFO[p.type].label}</th>`).join("")}</tr><tr><th>期間</th>${ps.map(p=>`<td>${TYPE_INFO[p.type].term}</td>`).join("")}</tr><tr><th>料金</th>${ps.map(p=>`<td>${price(p)}</td>`).join("")}</tr><tr><th>家具</th>${ps.map(p=>`<td>${p.furnished?"あり":"なし"}</td>`).join("")}</tr><tr><th>共用部</th>${ps.map(p=>`<td>${p.common?"あり":"なし"}</td>`).join("")}</tr></table></section><section class="section"><div class="property-list">${ps.map(p=>property(p,true)).join("")}</div></section>`}</main>`,"compare")}function inspector(){const types=Object.keys(TYPE_INFO);ins.innerHTML=`<h2 class="debug-title">デモの裏側</h2><p class="notice">総在庫 ${PROPS.length}件。ホテルは全体の${Math.round(PROPS.filter(p=>p.type==="hotel").length/PROPS.length*100)}%。</p><div class="debug-section"><h3>PROPERTY TYPES</h3>${types.map(t=>`<div class="kv"><span>${TYPE_INFO[t].label}</span><span>${PROPS.filter(p=>p.type===t).length}件</span></div>`).join("")}</div><div class="debug-section"><h3>SEARCH STATE</h3><div class="kv"><span>期間</span><span>${DURATIONS.find(d=>d.days===+state.filters.duration)?.label}</span></div><div class="kv"><span>種別</span><span>${state.filters.type==="all"?"すべて":TYPE_INFO[state.filters.type].label}</span></div><div class="kv"><span>表示可能</span><span>${filtered().length}件</span></div></div><div class="debug-section"><h3>IDENTITY</h3><div class="kv"><span>Guest</span><span>${state.guestId}</span></div><div class="kv"><span>User</span><span>${state.userId||"未マージ"}</span></div></div><div class="debug-section"><h3>EVENT LOG</h3>${state.events.slice(0,10).map(e=>`<div class="log">${e.time} ${e.type} ${e.id||e.to||""}</div>`).join("")}</div>`}function render(){try{if(state.screen.startsWith("detail:"))app.innerHTML=detail(state.screen.split(":")[1]);else app.innerHTML=({home,search,quiz,reasons,insight,recommend,compare}[state.screen]||home)();inspector()}catch(err){app.innerHTML=`<div class="page"><section class="section"><h1 class="blue-title">表示エラー</h1><p>${err.message}</p></section></div>`}}document.addEventListener("change",e=>{if(e.target.id==="area")state.filters.area=e.target.value;if(e.target.id==="rent")state.filters.maxRent=+e.target.value;if(e.target.id==="duration")state.filters.duration=+e.target.value;if(e.target.id==="type")state.filters.type=e.target.value;save()});document.addEventListener("click",e=>{const t=e.target.closest("button,[data-open],[data-pick]");if(!t)return;if(t.dataset.go)return go(t.dataset.go);if(t.id==="searchBtn")return go("search");if(t.dataset.duration){state.filters.duration=+t.dataset.duration;save();return go("search")}if(t.dataset.brand){state.filters.type=t.dataset.brand;save();return go("search")}if(t.dataset.open){event("property_view",{id:t.dataset.open});return go(`detail:${t.dataset.open}`)}if(t.dataset.pick){const p=PROPS.find(x=>x.id===t.dataset.pick);Object.entries(p.features).forEach(([k,v])=>state.profile[k]=Math.min(1,state.profile[k]+v*.08));state.filters.duration=p.minDays;event("quiz_pick",{id:p.id});return go("reasons")}if(t.dataset.reason){const k=t.dataset.reason,i=state.selectedReasons.indexOf(k);i<0?(state.selectedReasons.push(k),state.profile[k]=Math.min(1,state.profile[k]+.15)):state.selectedReasons.splice(i,1);save();return render()}if(t.hasAttribute("data-finish"))return go("insight");if(t.dataset.fav){e.stopPropagation();const i=state.favorites.indexOf(t.dataset.fav);i<0?state.favorites.push(t.dataset.fav):state.favorites.splice(i,1);event(i<0?"favorite_add":"favorite_remove",{id:t.dataset.fav});return render()}if(t.dataset.comp){e.stopPropagation();const i=state.compare.indexOf(t.dataset.comp);if(i<0&&state.compare.length<3)state.compare.push(t.dataset.comp);else if(i>=0)state.compare.splice(i,1);event(i<0?"compare_add":"compare_remove",{id:t.dataset.comp});return render()}if(t.hasAttribute("data-login")){if(!state.userId){state.userId=uid("user_");event("guest_merge",{id:state.userId})}return render()}});event("demo_open",{inventory:PROPS.length});render()})();